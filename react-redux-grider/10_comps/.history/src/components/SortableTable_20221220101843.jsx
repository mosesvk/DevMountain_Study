
import Table from '../components/Table';

const SortableTable = ({data, config}) => {
    let sortedData


  return (
    <div data={sortedData} config={config}>SortableTable</div>
  )
}

export default SortableTable